

<%= p current_cart %>

<%= puts current_cart %>

<%= print current_cart %>

<%= form_for @product, :url => product_path(@product), :method => :patch do |f| %>
	<%= f.label :user_name, "姓名:" %>
	<%= f.text_field :user_name %><br>
	<%= f.label :telephone, "電話:" %>
	<%= f.text_area :telephone %><br>


	<%= f.submit "確認結帳" %>
<% end %>