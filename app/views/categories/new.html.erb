<%= form_for @category, :url => categories_path do |f| %>
	<%= f.label :name, "Name" %>
	<%= f.text_field :name %>

	<%= f.submit "Create" %>
<% end %>

<%= link_to 'Back to manage category', categories_path %>