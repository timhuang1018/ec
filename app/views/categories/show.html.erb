<%= @category.name %><br>


		<% @need_products.each do |cp| %>
	<li>

		<%= cp.name %>
		<%= form_for cp, :url => { :controller => 'categories', :action => 'product_update'} do |f| %>
		<%= f.hidden_field :id %>
		<%= f.collection_select(:category_id, Category.all, :id, :name) %>

		<%= f.submit "Change" %>
		<% end %>
	</li>
		<% end %>




<%= link_to 'Back to manage category', categories_path %>