<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <%= link_to "EC shopping", '#', id: "logo" %> 
    <nav>
      <% if user_signed_in? %>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Home", managers_path %></li>
        <% if user_signed_in? %>
          <li><%= link_to('Check date', show_date_path) %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", uniuser_path(current_user) %></li>
              <li><%= link_to "Settings", '#' %></li>
              <li><%= link_to('購物紀錄', buy_record_managers_path(current_user)) %></li>
              <li class="divider"></li>
              <li>
                <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
              </li>
            </ul>
          </li>
        <% else %>
          <li><%= link_to 'Sign up', new_user_registration_path %></li>
        <% end %>
      </ul>
      <% end %>
    </nav>
  </div>
</header>