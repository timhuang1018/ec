<%= form_for @product, :url => product_path(@product), :method => :patch do |f| %>

<%= render :partial => 'layouts/product', :locals => { :f => f} %>

	<%= f.select :category_id, Category.all.map{ |c| [c.name, c.id] } %><br>

	<%= f.submit "Update" %>
<% end %>